# Primordial Seeds to Cosmic Structure

## Run

From this folder:

```bash
python3 -m http.server 8000
```

Open `http://localhost:8000` in a browser.

Using an HTTP server is required because the presentation loads local CSV and JSON data at runtime.
